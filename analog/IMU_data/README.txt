Every file in this folder has data ordered the following way:

Seconds | [yaw, pitch, roll] (in radians) | [x,y,z] (acceleration in m/s^2)

The rows in each file are in reverse order from time of implementation (so the first datapoint is at the bottom)
